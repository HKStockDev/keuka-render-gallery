# Optional single-password protection (HTTP Basic Auth — leave unset to disable)
GALLERY_PASSWORD=

# Projects directory for scan preview/admin (defaults to ./sample-projects)
PROJECTS_ROOT=

# Enable scan API on self-hosted / Windows Server (auto-enabled in dev; disabled on Vercel)
ENABLE_SCAN_API=true
